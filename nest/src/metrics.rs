//! Per-roost observability — RSS, CPU, fd count, wall-clock age.
//!
//! Stub filled in by Tier 1 agent. The Roost as a system resource needs
//! a window into what it's consuming; this module is that window.

// TODO: RoostMetrics struct, sampler task, Nest::inspect_metrics(sid).
